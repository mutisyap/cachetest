package ke.natujenge.recordproducer;

import ke.natujenge.cache.HashMapCache;
import org.junit.Test;

public class FakeRecordProducerTest {
    FakeRecordProducer fakeRecordProducer = new FakeRecordProducer(new HashMapCache());

    @Test
    public void testFakeProducer() throws InterruptedException {
//        fakeRecordProducer.generateRecords();
    }
}
